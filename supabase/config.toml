# Supabase local development config
project_id = "gp-link"

[functions.mypvit-secret]
verify_jwt = false

[functions.mypvit-webhook]
verify_jwt = false

[functions.mypvit-initiate]
# JWT vérifié manuellement dans le code via auth.getUser() pour éviter
# l'erreur "Unsupported JWT algorithm ES256" du runtime Edge
verify_jwt = false
